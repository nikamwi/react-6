import React from 'react'

export const UsersList = () => {
  return (
    <div>
         {/* {users.map((user) => (
          <div key={user._id}> {user.firstName} {user.lastName} {user.age} {user.email} {user.sex}
          <button onClick={() => {
              setIsUserUpdating(true)
              onEdit(user._id)
            }}>
              edit
            </button>
            <button onClick={() => onDelete(user._id)}>delete</button></div>
        ))} */}
    </div>
  )
}
